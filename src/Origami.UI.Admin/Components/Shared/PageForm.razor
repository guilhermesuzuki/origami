@inherits BasicFormFullContent<OrigamiPage, HubContentPage>

<div class="form @Class">
    <ContentHistories Class="p-3" Entity="this.Entity.Entity"></ContentHistories>
    <div class="row">
        <div class="col-12">
            <div class="p-3">
                <MudTextField ReadOnly="true"
                              Immediate="true"
                              Value="@(Text.Lower(Entity.Entity.IsDraft.YesNo()))"
                              HelperText="@Text.Lower("Still a draft?")"
                              Class="cursor-pointer yes-no"
                              title="@Text.Lower("Click here to change it")"
                              @onclick="() => this.Entity.Entity.IsDraft = !this.Entity.Entity.IsDraft.GetValueOrDefault()"></MudTextField>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12 col-xxl-6">
            <div class="p-3">
                <BasicHeaderNotes Entity="this.Entity.Entity"></BasicHeaderNotes>
                <div class="parent-selector mt-3">
                    @if (this.Entity.Parent != null)
                    {
                        <div class="p-3 border">
                            <div class="p-3 border">
                                <ContentHeaderInline Class="" Entity="this.Entity.Parent"></ContentHeaderInline>
                            </div>
                            <div class="btn-group mt-2 w-100">
                                <button class="btn btn-light w-100" @onclick="() => this.ShowParentSelector = !this.ShowParentSelector">
                                    <i class="fa-solid fa-square-check"></i>
                                    @Text.Lower("Select parent page")
                                </button>
                                <button class="btn btn-light w-100" @onclick="() => { this.Entity.Entity.ParentId = null; this.Entity.Parent = null; }">
                                    <i class="fa-solid fa-trash-can"></i>
                                    @Text.Lower("Clear selection")
                                </button>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="p-3 border">
                            <div class="btn-group w-100">
                                <button class="btn btn-light w-100" @onclick="() => this.ShowParentSelector = !this.ShowParentSelector">
                                    <i class="fa-solid fa-square-check"></i>
                                    @Text.Lower("Select parent page")
                                </button>
                            </div>
                        </div>
                    }
                    @if (ShowParentSelector)
                    {
                        <div class="mt-3">
                            <MudDataGrid T="OrigamiPage" Items="this.GetParents()" Filterable="false" SortMode="SortMode.Single" Class="border" Bordered="true" SelectOnRowClick="false">
                                <Columns>
                                    <TemplateColumn Title="@Text.Upper("Title")" Sortable="true" Filterable="false" CellClass="col-8" SortBy="x => x.Title">
                                        <CellTemplate>
                                            <ContentHeaderInline Class="" Entity="context.Item"></ContentHeaderInline>
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Sortable="false" Filterable="false" CellClass="col-1 text-center">
                                        <CellTemplate>
                                            <button class="cursor-pointer p-2" @onclick="() => { this.SetParent(context.Item); this.ShowParentSelector = false; }"><i class="bi bi-check-square-fill"></i></button>
                                        </CellTemplate>
                                    </TemplateColumn>
                                </Columns>
                                <PagerContent>
                                    <MudDataGridPager T="OrigamiPage" PageSizeOptions="[5]" />
                                </PagerContent>
                                <NoRecordsContent>
                                    <NoEntriesWereFound></NoEntriesWereFound>
                                </NoRecordsContent>
                            </MudDataGrid>
                        </div>
                    }
                </div>
                <BasicHeader Class="mt-3" Entity="this.Entity.Entity"></BasicHeader>
                <ul class="inline-list mt-3">
                    <li>
                        <MudTextField Immediate="true"
                                      AdornmentIcon="@Icons.Material.Filled.Description"
                                      Adornment="Adornment.Start"
                                      @bind-Value="@Entity.Entity.Title"
                                      HelperText="@Text.Lower("Title")"
                                      MaxLength="255"></MudTextField>
                    </li>
                    <li>
                        <LanguageWrittenOn Entity="this.Entity.Entity"></LanguageWrittenOn>
                    </li>
                </ul>
                <ul class="inline-list mt-3">
                    <li class="pe-2">
                        <MudTextField Immediate="true"
                                      AdornmentIcon="@Icons.Material.Filled.Link"
                                      Adornment="Adornment.Start"
                                      @bind-Value="@Entity.Entity.Slug"
                                      HelperText="@Text.Lower("Slug")" 
                                      MaxLength="255"></MudTextField>
                    </li>
                    <li style="width:140px;">
                        <button class="btn btn-secondary" @onclick="() => Entity.Entity.Slug = Entity.Entity.Title.GetSlug()" style="line-height: 15px;">
                            @Text.Lower("Create slug")
                            <span class="x-small">@Text.Lower("Based on title")</span>
                        </button>
                    </li>
                </ul>
                <ul class="inline-list mt-3">
                    <li>
                        <MudTextField Immediate="true"
                                      @bind-Value="@(Entity.Entity.Keywords)"
                                      HelperText="@Text.Lower("Keywords")" 
                                      MaxLength="1024"></MudTextField>
                    </li>
                </ul>
            </div>
        </div>
        <div class="col-12 col-xxl-6">
            <div class="text-center p-3">
                <HeaderImageLite Entity="@Entity.Entity"></HeaderImageLite>
                <MudFileUpload T="IBrowserFile" FilesChanged="UploadHeader" Accept=".png, .jpg, .bmp, .tiff" MaximumFileCount="1">
                    <CustomContent>
                        <div class="btn-group mt-2 w-100">
                            <button class="btn btn-light w-100" @onclick="context.OpenFilePickerAsync">
                                <i class="fa-solid fa-cloud-arrow-up"></i>
                                <div>@Text.Lower("Only image files")</div>
                                <div class="x-small">@Text.Original("{0}MB max.", 6)</div>
                            </button>
                            <button class="btn btn-light w-100" @onclick="() => this.FileManagerForImages = true">
                                <i class="fa-solid fa-folder"></i>
                                @Text.Lower("Pick from file manager")
                            </button>
                            <button class="btn btn-light w-100" @onclick="ClearHeader">
                                <i class="fa-solid fa-trash-can"></i>
                                @Text.Lower("Clear header")
                            </button>
                        </div>
                    </CustomContent>
                    <SelectedTemplate></SelectedTemplate>
                </MudFileUpload>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="p-3">
                <MudTextField FullWidth="true"
                              Immediate="true"
                              @bind-Value="@Entity.Entity.Description"
                              HelperText="@Text.Lower("Description")"
                              MaxLength="2048"
                              MaxLines="16"></MudTextField>
            </div>
            <div class="p-3">
                <TinyMCEContent Entity="Entity.Entity" Title="@Text.Lower("Content")"></TinyMCEContent>
            </div>
        </div>
    </div>
    <div class="buttons mt-3 p-3 text-center position-sticky z-index-middle" style="bottom:0;">
        <div class="btn-group">
            <button class="btn btn-success text-white fw-bold" @onclick="Save" disabled="@this.DisableTheSaveButton">
                <i class="fa-solid fa-floppy-disk"></i>
                @Text.Lower("Save")
            </button>
            <button class="btn btn-secondary text-white fw-bold" @onclick="UndoChanges" disabled="@this.Entity.Entity.New">
                <i class="fa-solid fa-rotate-left"></i>
                @Text.Lower("Undo changes")
            </button>
            <button class="btn btn-info text-white fw-bold" @onclick="CreateEntity">
                <i class="fa-solid fa-square-plus"></i>
                @Text.Lower("New")
            </button>
        </div>
    </div>
</div>
@if (FileManagerForImages)
{
    <div class="position-fixed popup-container">
        <div class="border shadow-lg position-relative" style="width: 90%; height: 90%;">
            <div class="overflow-auto" style="height:100%; padding-bottom: 70px;">
                <FileManager Mode="Image" OnFileClicked="this.ImageFromFileManagerWillAssignEntityHeader"></FileManager>
            </div>
            <ul class="inline-list position-absolute w-100 border bg-white" style="bottom:0; z-index: 50;">
                <li></li>
                <li class="text-right">
                    <div class="btn-group p-3">
                        <button class="btn btn-secondary" @onclick="() => this.FileManagerForImages = false">
                            <i class="fa-solid fa-circle-xmark"></i>
                            @Text.Lower("Close window")
                        </button>
                    </div>
                </li>
            </ul>
        </div>
    </div>
}
@code {
    
}
