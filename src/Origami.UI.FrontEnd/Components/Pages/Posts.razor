@page "/blogs/{BlogSlug}/posts"
@inherits BasicPage

<Root>
    <BlogHeaderLite Class="text-center fs-1" Entity="this.GetBlogFromSlug()" DisplayBlogNameEvenWithoutHeaderImage="false"></BlogHeaderLite>
    <PostList Class="mt-3" BlogId="@BlogId" Page="@Page"></PostList>
</Root>

@code {
    [Parameter] public int Page { get; set; } = 1;

    protected override void OnParametersSet()
    {
        base.OnParametersSet();
        this.BlogId = this.GetBlogFromSlug().Id;
    }

    protected override void SetPageTitle()
    {
        this.PageTitle.SetTitle(Text.Get("Posts"));
    }
}
